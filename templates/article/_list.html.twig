<div class="list-group list-group-flush">
    {% for article in articles %}
        <a class="list-group-item"
           href="{{ path('app_article_index', {'slug': article.slug}) }}">
            {{ article.title }}
        </a>
    {% else %}
        <p>Aucun article.</p>
    {% endfor %}
</div>
